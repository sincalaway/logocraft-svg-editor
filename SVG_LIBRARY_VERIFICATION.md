# SVG 图库验收记录

本轮功能审查移除了未被界面调用的调色板常量、历史与快捷键弹层状态、冗余旋转处理器及其对应状态更新；既有编辑、历史、导出、模板和多画板功能均保留，因为它们分别承担明确的工作流角色。

新增的 **SVG 图库** 是浏览器本地资产库，提供上传、缩略图查看、插入画板、重命名与删除能力。实际验收中，上传了一个纯矢量 SVG 样本，将其插入为第五个画板图层，并成功导出优化 SVG；交付预检将该图库资产识别为矢量内容，而非位图图层。

图库会移除脚本、位图、外部引用和动画元素，单个资产上限 800 KB、最多 36 个资产。资产仅保存在当前浏览器；需要迁移时，请保留原始 SVG 文件后重新上传。
