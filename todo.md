# LogoCraft 编辑器增强待办

- [x] 梳理现有画布、历史记录与元素状态的数据流。
- [x] 支持空白区域框选多个元素，并支持 Shift 追加或取消选择。
- [x] 支持滚轮围绕指针缩放画布并提供缩放状态。
- [x] 为选中对象提供八向拖拽尺寸调整控制点。
- [x] 在右侧实现可拖拽排序、锁定与隐藏的图层管理器。
- [x] 将编辑内容自动持久化为本地草稿并提供恢复提示。
- [x] 提供基础图形组合预设模板并支持一键应用。
- [x] 完成类型检查、生产构建和视觉复核。

## 高级排版、分组与工程管理

- [x] 梳理当前元素选择、图层顺序与本地草稿数据结构。
- [x] 实现对齐、等距分布和拖拽时的智能吸附参考线。
- [x] 增加图层分组，并在图层管理器中呈现组层级。
- [x] 支持对多选图层批量设置颜色、锁定与隐藏状态。
- [x] 支持多个命名草稿的创建、保存、切换和删除。
- [x] 支持 JSON 工程文件的导入和导出。
- [x] 完成类型检查、生产构建和视觉复核。

## 精细变换与历史操作

- [x] 评估当前画板在桌面和窄屏下的尺寸与可读性。
- [x] 实现旋转控制柄，并支持 15° 增量与 45° 主轴的智能吸附。
- [x] 支持图层样式复制与粘贴。
- [x] 增加全局快捷键说明与快捷操作。
- [x] 记录操作描述，并实现可点击回退的历史面板。
- [x] 完成类型检查、生产构建和视觉复核。

## 批量变换、参考线与历史快照

- [x] 梳理多选对象边界、变换基准点、参考线和历史记录的数据模型。
- [x] 实现多选元素围绕共同中心的整体旋转与等比缩放。
- [x] 支持从标尺创建、拖动定位和删除水平/垂直参考线。
- [x] 将用户参考线纳入吸附系统和本地工程持久化。
- [x] 支持为历史记录命名并收藏关键快照。
- [x] 完成类型检查、生产构建和视觉复核。

## 参考线控制与收藏快照

- [x] 梳理参考线属性、多选对齐目标和收藏快照数据模型。
- [x] 实现参考线自定义颜色、锁定与全局显示/隐藏控制。
- [x] 支持多选元素按边缘或中心对齐到指定参考线。
- [x] 为收藏快照保存缩略图预览与导出内容。
- [x] 在历史记录面板展示收藏缩略图并支持直接导出。
- [x] 完成类型检查、生产构建和视觉复核。

## 画板配置、参考线预设与快照差异

- [x] 梳理画板尺寸、出血、参考线预设和快照对比的数据模型。
- [x] 实现自定义画板宽高、预设尺寸与出血区域设置。
- [x] 将画板与出血配置纳入本地工程和 JSON 文件持久化。
- [x] 支持保存、命名、应用和删除参考线布局预设。
- [x] 实现两个历史快照的并排预览与图层变化摘要。
- [x] 完成类型检查、生产构建和视觉复核。

## 多画板、单位与版本对比

- [x] 梳理画板资产、历史快照、单位显示和 GitHub 远程仓库状态。
- [x] 实现同一工程内多画板的新建、重命名、切换和删除。
- [x] 将元素、参考线及画板配置隔离并持久化到各画板。
- [x] 实现快照的像素级叠加差异图。
- [x] 为标尺、坐标和尺寸字段提供 px / mm / in 单位切换。
- [x] 美化多画板和版本对比相关界面。
- [x] 完成类型检查、生产构建和视觉复核。
- [x] 提交并上传最终代码到 GitHub 仓库。

## 批量交付、画板模板与差异精调

- [x] 梳理多画板导出、模板复用和差异图控制所需的数据流。
- [x] 实现多画板批量导出，并支持 SVG 和 PNG 格式选择。
- [x] 支持将当前画板保存为命名共享模板并快速应用。
- [x] 为像素差异图增加阈值和透明度调节。
- [x] 完成类型检查、生产构建和视觉复核。

## 导出打包、模板共享、差异标注与独立仓库

- [x] 梳理批量导出命名、模板文件和差异标注的数据模型与仓库状态。
- [x] 实现自定义文件命名规则和多画板 ZIP 打包下载。
- [x] 实现共享画板模板的 JSON 导入和导出。
- [x] 在差异图中支持区域框选、高亮和文字标注。
- [x] 撰写 README 与独立部署指南。
- [x] 完成类型检查、生产构建和视觉复核。
- [x] 创建独立 GitHub 仓库并推送最终代码。

## 部署指南与开源发布

- [x] 核验现有部署文档、构建目录和独立仓库配置。
- [x] 在 README 中补充 Cloudflare Pages 与 Cloudflare Workers 静态托管步骤。
- [x] 在部署指南中补充 Nginx 自托管服务器配置与 HTTPS 建议。
- [x] 添加 MIT 开源许可证并更新 README 的许可证说明。
- [x] 核验文档并提交更新到独立 GitHub 仓库。

## 持续集成、部署徽章与版本发布

- [x] 核验仓库脚本、分支和现有文档结构。
- [x] 添加 GitHub Actions 类型检查与生产构建工作流。
- [x] 在 README 中添加 Cloudflare Pages 部署徽章。
- [x] 添加变更日志和版本发布规范。
- [x] 核验工作流语法并提交到独立 GitHub 仓库。

## 自动发布、预览部署与依赖维护

- [x] 核验现有 CI、版本标签与 Cloudflare 部署文档。
- [x] 添加版本标签触发的 GitHub Release 自动创建工作流。
- [x] 添加 Cloudflare Pages 预览部署工作流与所需 Secrets 说明。
- [x] 添加 Dependabot 的 npm 与 GitHub Actions 更新策略。
- [x] 核验自动化配置并推送独立 GitHub 仓库。

## 新手引导、效率提示与本地恢复

- [x] 梳理首次访问状态、工具按钮和本地工程保存数据流。
- [x] 实现展示多画板、历史差异与精确编辑功能的新手引导弹窗。
- [x] 在侧边栏提供快捷键说明，并为工具按钮补齐悬停提示。
- [x] 强化本地自动保存、刷新恢复和最近保存状态反馈。
- [x] 完成类型检查、生产构建和视觉复核。

## 命令中心、导出质量与版本恢复

- [x] 梳理现有快捷键、导出流程、工程自动保存与检查点展示配置。
- [x] 实现可搜索、可通过快捷键唤起的全局命令面板。
- [x] 实现 SVG 代码优化与品牌规范预检，并接入导出流程。
- [x] 实现包含缩略图预览、恢复和删除操作的持久化版本列表。
- [x] 更新项目展示标签并完成类型检查、构建和视觉复核。

## 检查点标签整理

- [ ] 确认管理界面中的旧检查点内容标签来源。
- [ ] 移除或替换可控制的展示标签，并复核最新展示状态。

## Cloudflare Pages 部署指引

- [x] 核验生产构建命令、输出目录和现有 Cloudflare 配置。
- [x] 整理 GitHub 连接部署、环境变量、预览部署与自定义域名步骤。

## Pages 部署前验收

- [x] 验证类型检查、生产构建和静态发布目录。
- [x] 核验核心工具按钮、导出、自动保存和版本恢复流程。
- [x] 检查 Cloudflare Pages 的静态资源与路由兼容性，并给出结论。

## Pages 首次部署操作

- [x] 整理连接 GitHub、配置 main 分支与 dist/public 发布目录的控制台步骤。

## 100% 缩放视口适配

- [x] 分析画板缩放、侧栏和工作区的有限视口约束。
- [x] 修复 100% 缩放时画板与关键控制区无法完整查看的问题。
- [x] 验证桌面及窄屏显示，并同步修复到 GitHub。

## 缩放偏好与画板定位

- [x] 梳理工程持久化数据与现有缩放、工作区滚动状态。
- [x] 实现缩放百分比输入和超大画板一键居中。
- [x] 保存并恢复每个工程的手动缩放偏好。
- [x] 完成类型检查、浏览器验证和仓库同步。

## 功能精简与 SVG 图库

- [x] 审查重复、无入口或与核心编辑工作流重叠的功能。
- [x] 移除确认无实际价值的重复入口，并保留核心编辑能力。
- [x] 实现本地 SVG 图库上传、插入、重命名和删除。
- [x] 验证图库的本地保存、编辑器插入和仓库同步。

## 分类 SVG 静态图库接入

- [x] 安全检查 RAR 目录结构、SVG 数量、文件体积和异常内容。
- [x] 规划 GitHub 与 Cloudflare Pages 兼容的分类静态资源目录。
- [x] 导入用户 SVG 资源并将分类索引接入图库。
- [x] 验证生产构建和静态资源访问后同步仓库。

## 图库搜索与我的收藏

- [x] 梳理分类图库清单与本地收藏数据的持久化边界。
- [x] 实现关键词搜索、分类筛选与结果状态提示。
- [x] 实现 SVG 收藏切换、我的收藏分类和浏览器本地恢复。
- [x] 完成类型检查、浏览器验证和 GitHub 同步。

## 收藏分组与图库浏览性能

- [x] 梳理现有收藏本地存储和分类图库渲染数据流。
- [x] 实现收藏分组创建、重命名、删除与素材归类。
- [x] 实现 SVG 悬停放大预览与分批滚动加载。
- [x] 完成类型检查、浏览器性能验证和 GitHub 同步。

### 验证记录

- 已通过 `pnpm check` 与 `pnpm build`；生产构建无错误。
- 桌面端已确认分类卡片悬停时显示纸张网格放大预览。
- 已验证测试分组创建、素材归类、重命名和删除；删除分组不会移除原有收藏素材。
- 已验证分组刷新后从 `logocraft-svg-catalog-favorite-groups-v1` 自动恢复。
- 已在含 311 个素材的 symbols 分类确认首批渲染 64 个，滚动后自动扩展至 128 个。
- 已清理验证过程中的临时分组，保留原有收藏状态。

## 右侧检查器完整显示修复

- [x] 定位编辑器三栏布局在常见桌面宽度下的挤压与裁切来源。
- [x] 调整右侧检查器、左侧资产栏和画布工作区的响应式显示策略。
- [x] 验证 1280px 桌面、窄桌面与平板宽度下的完整可用性并同步仓库。

### 验证记录

- 1024px 至 1279px 宽度下，右侧检查器改为稳定的 250px 至 278px 响应式列，不再依赖 1280px 才显示的断点。
- 在 1280px 工作区实测检查器右边界为 1280px，页面无横向溢出；类型检查与生产构建均通过。

## 右侧检查器效率增强

- [x] 梳理现有右侧检查器、工具按钮与响应式断点状态。
- [x] 实现桌面侧栏折叠控制与按设备宽度保存的面板偏好。
- [x] 实现平板检查器抽屉、打开入口与触控友好关闭流程。
- [x] 完成桌面、平板布局验证与 GitHub 同步。

### 验证记录

- 已验证桌面检查器可折叠并在折叠后立即释放画布宽度，恢复入口始终保持可见。
- 已验证宽度控制可在当前宽屏设备档位切换至 344px，并写入 `logocraft-inspector-layout-v1`。
- 平板 768px 至 1023px 使用固定右侧抽屉、遮罩关闭与 Escape 关闭流程；桌面和各设备档位的偏好相互隔离。
- 已在 1100px 窄桌面确认右侧检查器、折叠轨道与画布可同时完整呈现；在 800px 平板确认默认隐藏检查器并保留右下角抽屉入口。

## 右侧检查器拖拽调宽

- [x] 复核桌面检查器现有宽度档位与各设备档位边界。
- [x] 实现可访问的拖拽调整手柄、宽度限制与拖拽态反馈。
- [x] 验证拖拽后的设备级宽度保存、恢复与平板降级行为，并同步仓库。

### 验证记录

- 已验证指针拖拽可将当前宽屏设备档位的检查器从 344px 调整至 388px，并保存至 `logocraft-inspector-layout-v1`。
- 拖拽手柄支持方向键微调；向左键使右侧栏增加 12px，已验证至 400px。
- 已刷新页面确认 400px 宽度自动恢复；平板端继续沿用抽屉交互，不显示桌面拖拽手柄。

## 右侧检查器 100% 缩放完整访问

- [x] 定位检查器内容区、缩放控制区与视口高度之间的裁切约束。
- [x] 将检查器主体改为独立滚动区，并固定保留必要的底部控制入口。
- [x] 验证 100% 缩放、低高度视口与平板抽屉中所有检查器内容均可访问，并同步仓库。

### 验证记录

- 已在 1280 × 961（100% 桌面高度）确认工程草稿、画布缩放、适配与居中控制均完整可见。
- 已在 1280 × 720 低高度下确认检查器底部缩放和恢复控制仍固定可达，主体内容保留独立滚动空间。
- 实测检查器从导航栏下方延伸至视口底部，页面无垂直溢出；主体与底部控制区各自拥有可控滚动边界。

## 左侧素材栏收起与展开

- [x] 梳理左侧工具轨、素材栏与画布工作区之间的布局边界。
- [x] 实现左侧素材栏收起/展开入口、过渡状态与设备级偏好保存。
- [x] 验证折叠时画布工作区释放、素材功能不受影响及平板降级策略。
- [ ] 保存发布修复版本并同步 GitHub 仓库。

### 验证记录

- 已验证桌面端“收起素材栏”会隐藏仅包含图形、文字和预设的素材面板，同时保留 58px 主工具轨。
- 收起后画布立即获得原素材栏的宽度，边缘入口切换为“展开素材栏”；展开后素材卡片和标签完整恢复。
- 折叠状态使用 `logocraft-asset-tray-layout-v1` 按宽屏与窄桌面档位分别保存；小于 1024px 时素材栏本就隐藏，不额外显示控制轨。
- 已实测宽屏档位的收起偏好写入后，素材栏宽度在过渡完成时降至 1px，随后可恢复默认展开状态。

## 双侧栏回归与左侧拖拽调宽

- [x] 定位左侧栏控制升级后导致右侧检查器不可见的布局或偏好冲突。
- [x] 恢复右侧检查器默认可见状态，并保留其折叠、抽屉和调宽能力。
- [x] 实现左侧素材栏可访问的拖拽调宽、边界限制与设备级宽度保存。
- [x] 验证两侧栏共存、刷新恢复、平板降级与画布工作区稳定性，并同步 GitHub。

### 验证记录

- 已将右侧检查器偏好迁移至 `logocraft-inspector-layout-v2`，使旧的折叠状态不再影响新版本默认显示。
- 已验证右侧检查器恢复默认显示（当前宽度 308px），与左侧素材栏同时可用。
- 已验证左侧手柄拖拽可将素材栏调整至 280px，并写入 `logocraft-asset-tray-layout-v2`；方向键可微调至 292px。
- 已刷新确认右侧检查器仍显示、左侧宽度按设备偏好恢复；在 1100px 窄桌面确认两侧栏、手柄和画布可同时使用。

## 右侧检查器原有编辑能力回归

- [x] 对照原检查器恢复图层、文本内容、字号、坐标和尺寸属性入口。
- [x] 恢复工程草稿、画布缩放、适配、居中与还原控制的完整访问。
- [x] 验证文本和图形选中状态下的检查器编辑、独立滚动和双侧栏共存。
- [x] 保存回归修复版本并同步 GitHub 仓库。

### 验证记录

- 已确认选中文本对象后，检查器仍显示“内容”“字号”“X/Y”“宽度/高度”等原有字段；选中图形时保留填充、旋转和几何字段。
- 已确认工程草稿、缩放输入、适配、居中与还原控制均完整可访问。
- 已恢复右侧顶部“SVG 图库”快捷入口，且已实际打开分类图库。
- 已在文本和图形对象之间切换，确认检查器会按对象类型切换字段且两侧栏同时保持可用。
- 已在 1100px 窄桌面完成视觉复核：检查器完整呈现且图层、属性、工程草稿与缩放控制均可访问。

## 双侧栏悬浮控制统一

- [x] 审查左右侧栏现有收起、展开与平板抽屉触发入口。
- [x] 统一桌面端左右悬浮控制轨的视觉、标签和收起状态提示。
- [x] 完善收起后的一键恢复、悬浮提示与平板抽屉入口。
- [x] 验证双侧栏、画布专注模式与平板抽屉工作流，并同步 GitHub。

### 验证记录

- 已验证左侧素材栏收起后，居中悬浮入口切换为“展开”，画布立即获得额外宽度。
- 桌面端左侧使用 `Alt + [`，右侧使用 `Alt + ]`；各控制的悬停提示已显示对应快捷键。
- 平板端右侧继续使用右下角悬浮入口打开和关闭抽屉检查器。
- 已验证左右栏可独立收起、独立通过边缘悬浮入口恢复；两栏同时收起时进入画布专注状态，恢复任一栏不会影响另一栏。

## SVG 图库入口去重与全项目回归

- [x] 审查检查器内嵌图库按钮与全局图库快捷入口的重复挂载。
- [x] 保留唯一的检查器顶部图库入口，并修复标题区在窄面板下的重叠。
- [x] 回归验证双侧栏、检查器编辑、SVG 图库、缩放和桌面/平板布局。
- [x] 保存修复版本并同步 GitHub 仓库。

### 验证记录

- 已定位重复原因：图库面板自带固定入口，额外挂载的全局快捷组件与其在检查器标题区重叠。
- 已移除重复全局组件；实际页面现在仅保留一个“SVG 图库”按钮，检查器标题、图层、属性和缩放区域无重叠。
- 已通过 `pnpm check`、生产构建、1100px 视觉复核和浏览器控制台检查；无新增运行时错误。
- 已实际点击唯一图库入口并成功打开分类素材库；800px 平板视口下图库入口与右下角检查器抽屉入口均完整可用且无互相遮挡。

## SVG 主色提取与悬浮预览清理

- [x] 审查 SVG 图库插入事件、矢量内容和检查器填充色的数据映射。
- [x] 实现安全的 SVG 主色提取，并在插入后写入选中对象的填充属性。
- [x] 修复插入、关闭和切换素材时的悬浮预览残留状态。
- [x] 验证多色 SVG、无填充 SVG 与图库交互生命周期，并同步 GitHub。

### 验证记录

- 通过主色验证素材确认多色 SVG 的检查器填充字段可显示 `#F43123`，不再使用默认深色。
- 通过 Abstract_01 渐变素材确认可识别 `stop-color`，检查器显示 `#4C7B2A` 并列出色板；无可识别颜色时才回退为 `#1C1A18`。
- 已验证素材悬停后插入会关闭图库且卸载预览面板；打开、关闭和切换图库时也会清理预览状态。
- 临时测试素材均已删除，用户工程恢复为原有 5 个图层；类型检查与生产构建已通过。

## SVG 色板与渐变编辑工具

- [x] 梳理已解析的 SVG 直接色、描边色与渐变 stop 色数据结构。
- [x] 实现点击色板切换主色和选中 SVG 内指定颜色的安全替换。
- [x] 实现渐变色完整列表、色值编辑和实时画布更新。
- [x] 验证多色、渐变和普通单色 SVG 的颜色编辑、撤销与导出，并同步 GitHub。

### 验证记录

- 已验证多色渐变 SVG 显示 4 个主题色色板和 6 个渐变停靠点；点击色板会立即替换当前 SVG 的主题色并刷新画布。
- 已验证替换工具可将选定主题色替换为 `#11CC88`，同步更新同色的渐变停靠点与检查器主色。
- 已验证渐变 a 的起始色可调整为 `#FF00AA`，色板、渐变列表和画布预览实时更新。
- 临时色彩测试素材已删除，用户工程恢复为原有 5 个图层；类型检查与生产构建通过。

## 内置复合标记双色色板

- [x] 梳理 LogoCraft 标记的主构件与琥珀定位点渲染、存储和导出路径。
- [x] 为内置复合标记建立可持久化的构件级颜色属性。
- [x] 在属性面板显示黑色主构件和琥珀定位点色板，并支持独立编辑。
- [x] 验证编辑、撤销、导出和历史工程兼容性，并同步 GitHub。

### 验证记录

- 选中内置 LogoCraft 标记时，属性面板已显示“标记构件色板：2 构件”，包括主构件 `#1C1A18` 与定位点 `#FF6B35`。
- 已验证定位点可独立调整为 `#00AEEF` 并实时反映在画布和色板中，随后已恢复为原始琥珀色。
- 新旧工程缺少 `accentFill` 时会安全回退为 `#FF6B35`；预览和导出采用同一定位点色值。

## 复合标记色彩工作流升级

- [x] 为主构件与定位点增加可切换的颜色联动锁定，并在双向编辑时保持综合色相关系。
- [x] 增加“一键恢复默认色”操作，恢复 `#1C1A18` 主构件与 `#FF6B35` 定位点，并写入历史记录。
- [x] 增加精密工作台风格的完整内置色板预设列表，可一键应用双构件配色方案。

### 验证记录

- 已在浏览器启用构件颜色联动；主构件从 `#1C1A18` 调整为 `#0F766E` 时，定位点自动更新为关联色 `#8BFAD8`。
- 已验证“默认色”将双构件恢复至 `#1C1A18 / #FF6B35`，并经过既有 `commit` 路径写入撤销和自动保存流程。
- 已验证“深海信号”预设会同步应用 `#123447 / #00A3B4`，之后已恢复用户工程的品牌基准配色；共有 8 组内置色板。

## SVG 多色定向编辑修复

- [x] 修订 SVG 色板点击行为：点击仅选中当前填充、描边或渐变色，不直接将图内多色归并为单一主题色。
- [x] 为当前选中色提供独立颜色输入与定向应用操作，仅修改 SVG 内对应的颜色实例。
- [x] 保留并明确渐变停靠点的逐点编辑能力，验证多色填充、描边与渐变 SVG 的提取、编辑、撤销和导出。

### 验证记录

- 已用包含直接填充、描边和线性渐变的临时 SVG 验证出 5 种提取色及 2 个渐变色点；点击色板只改变“当前选中色”，不修改画布。
- 已验证将选中的 `#059669` 描边定向应用为 `#7C3AED` 后，蓝色填充、橙色定位点和两枚渐变色均保持独立。
- 已验证渐变起始点可单独调整为 `#EAB308`，终点色保持 `#F4B860`；测试素材已删除，工程恢复为原有 5 个图层。

## SVG 色彩统计与替换范围

- [x] 在提取色板中显示当前颜色的填充、描边和渐变停靠点使用次数。
- [x] 为当前选中色增加“全部 / 仅填充 / 仅描边”替换范围选项，并保持渐变色点的独立编辑能力。
- [x] 验证同色填充与描边、渐变色点和混合范围 SVG 的统计、定向替换、撤销与导出结果。

### 验证记录

- 已验证同色 `#1E40AF` 正确统计为填充 2、描边 1、渐变 1；其他颜色分别显示独立的属性使用次数。
- 已验证“仅填充”将两处蓝色填充替换为 `#DB2777`，同时保留蓝色描边与渐变起始色。
- 已验证“仅描边”将一处蓝色描边替换为 `#0F766E`，同色渐变起始点仍保留为 `#1E40AF`；临时测试 SVG 已删除，工程恢复为原有 5 个图层。
