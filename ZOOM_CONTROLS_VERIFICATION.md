# 缩放控制与工程偏好验证

本轮已验证三项缩放工作流。首先，在缩放输入框键入 `130` 并提交后，编辑器准确切换到 **130% 手动缩放**。其次，等待自动保存完成后，本地工程数据已写入 `zoomPreference: { mode: "manual", value: 1.3 }`。最后，刷新页面后，编辑器重新载入时显示 **130%**，证明缩放偏好能按工程恢复。

在 130% 的超大画板状态下，“**居中画板**”会将工作区滚动到画板中心；“**适配画板**”仍可恢复到当前可用视口的完整可见比例。TypeScript 检查与生产构建均已通过。
