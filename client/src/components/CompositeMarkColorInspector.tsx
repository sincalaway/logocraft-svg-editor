/* Precision workbench module: exposes the independently editable construction colors of the built-in LogoCraft mark. */
import { CircleDot, Palette } from "lucide-react";

type Props = { primary: string; accent: string; onPrimaryChange: (color: string) => void; onAccentChange: (color: string) => void };

export default function CompositeMarkColorInspector({ primary, accent, onPrimaryChange, onAccentChange }: Props) {
  return <section className="border-t border-white/10 px-4 py-4"><div className="flex items-center justify-between"><p className="section-label">标记构件色板</p><span className="technical-number text-[8px] text-[#8d8a8d]">2 构件</span></div><p className="mt-1 text-[9px] leading-relaxed text-[#8e8b8f]">LogoCraft 标记由主构件和定位点组成，可独立提取、编辑与导出。</p><div className="mt-3 grid grid-cols-2 gap-2"><label className="inspector-field"><span className="flex items-center gap-1"><Palette size={10} /> 主构件</span><div className="flex h-8 items-center gap-2 border border-white/10 bg-[#252529] px-2"><input aria-label="编辑 LogoCraft 标记主构件颜色" type="color" value={primary} onChange={(event) => onPrimaryChange(event.target.value)} className="h-4 w-4 border-0 bg-transparent p-0" /><span className="technical-number text-[9px] font-bold text-[#ded8d1]">{primary.toUpperCase()}</span></div></label><label className="inspector-field"><span className="flex items-center gap-1"><CircleDot size={10} className="text-[#ff6b35]" /> 定位点</span><div className="flex h-8 items-center gap-2 border border-white/10 bg-[#252529] px-2"><input aria-label="编辑 LogoCraft 标记定位点颜色" type="color" value={accent} onChange={(event) => onAccentChange(event.target.value)} className="h-4 w-4 border-0 bg-transparent p-0" /><span className="technical-number text-[9px] font-bold text-[#ded8d1]">{accent.toUpperCase()}</span></div></label></div><div className="mt-3 flex items-center gap-1.5"><button type="button" title={`设为主构件色：${primary}`} className="h-5 w-5 border border-white/30" style={{ backgroundColor: primary }} onClick={() => onPrimaryChange(primary)} /><button type="button" title={`设为定位点色：${accent}`} className="h-5 w-5 border border-[#ff6b35]/60" style={{ backgroundColor: accent }} onClick={() => onAccentChange(accent)} /><span className="technical-number text-[8px] text-[#88858a]">主构件 / 定位点</span></div></section>;
}
